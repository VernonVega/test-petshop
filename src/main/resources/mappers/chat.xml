<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ChatMapper">
    <resultMap type="Chat" id="ChatResult">
        <id property="id" column="id"/>
        <result property="isEnabled" column="enabled"/>
        <result property="title" column="title"/>
        <result property="type" column="type"/>
    </resultMap>

    <select id="selectChatById" parameterType="int" resultMap="ChatResult">
        SELECT *
        FROM Chat
        WHERE id = #{id};
    </select>

    <select id="selectAllChats" resultMap="ChatResult">
        SELECT *
        FROM Chat
        ORDER BY id;
    </select>

    <insert id="addChat" parameterType="Chat">
        INSERT INTO Chat
        (id, enabled, title, type)
        VALUES
        (#{id}, #{isEnabled}, #{title}, #{type});
    </insert>

    <update id="updateChat" parameterType="Chat">
        UPDATE Chat SET
        enabled = #{isEnabled},
        title = #{title},
        type = #{type}
        WHERE id = #{id}
    </update>

    <delete id="deleteChatById" parameterType="long">
        DELETE FROM Chat
        WHERE id = #{id}
    </delete>
</mapper>